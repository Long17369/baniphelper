{
  // C++ 格式化必须走仓库根目录的 .clang-format。
  //
  // 用户级设置里 C_Cpp.formatting 是 vcFormat，那会让扩展使用自带的
  // VC++ 格式化器并完全忽略 .clang-format；而 vcFormat 的
  // indent.namespaceContents 默认为 true，现象就是命名空间内容被整体缩进一档。
  //
  // 这几项在这里的项目级固定下来，任何人克隆下来格式化结果都一致。
  "C_Cpp.formatting": "clangFormat",
  "C_Cpp.clang_format_useWorkspaceFormat": true,
  "C_Cpp.clang_format_style": "file",

  // 找不到 .clang-format 时不要静默套用某个内置风格，
  // 那会让「配置没生效」这件事看起来像「配置生效了但结果不对」。
  "C_Cpp.clang_format_fallbackStyle": "none",

  "[cpp]": {
    "editor.defaultFormatter": "ms-vscode.cpptools"
  },
  "[c]": {
    "editor.defaultFormatter": "ms-vscode.cpptools"
  },

  // LICENSE 是纯文本，不该被当成 Markdown。
  //
  // markdownlint 扩展没有「忽略文件」这类配置项，它只按「文档语言是 markdown」
  // 决定要不要检查。LICENSE 没有扩展名、被 VS Code 归到 Markdown，
  // 于是 GPL 正文产生 18 条误报：编号条款被读成有序列表，type `show w' 被读成代码跨度。
  //
  // 注意 .markdownlintignore 挡不住这件事：实测把文件显式交给 markdownlint-cli2 时，
  // 忽略表里的条目照样会被 lint。真正生效的是这里的语言归属。
  "files.associations": {
    "LICENSE": "plaintext"
  }
}
