# 本地环境与临时文件，不入库

tmp/

# 依赖
node_modules/

# 构建产物
build/
out/
compile_commands.json

# 本机专属的 CMake 预设覆盖（不入库，命令行构建用 CMakePresets.json）
CMakeUserPresets.json

# 编辑器与工具缓存
.cache/

# Qt 与 IDE 的本地文件。这些文件里含机器绝对路径，按项目规矩不入库。
# .qmlls.ini 由 QML 语言服务器生成，内容是构建目录与 Qt 安装位置。
.qmlls.ini
.qtc_clangd/
*.user
