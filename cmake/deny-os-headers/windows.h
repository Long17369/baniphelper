// 「禁止包含」占位文件，不是真正的系统头。
//
// 本目录在 -I 搜索路径中的位置早于编译器自带的系统头目录，因此 core 与 ui 里一旦写下
// #include <windows.h>，包含到的就是本文件，构建当场失败。
// 这是「核心层与界面层不得引用平台专有头」这条铁律的构建期强制手段，
// 依据见 docs/architecture.md 第 12.2 节。
#error "核心层与界面层不得包含平台专有头 <windows.h>：平台差异只能出现在 src/platform/ 下。"
