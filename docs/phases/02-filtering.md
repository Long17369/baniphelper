# 阶段二 封禁能力

## 1 目标与范围

把规则模型完整转化为 Windows 过滤平台的过滤器，覆盖 IPv4 与 IPv6、TCP 与 UDP、
出站与入站、端口与网段，并支持立即中断已建立的 IPv4 连接。

本阶段**不涉及界面与记录**，只保证「封得住、撤得净、重启还在」。

## 2 前置条件

| 项 | 状态 |
| --- | --- |
| 阶段一完成 | 接口已冻结，过滤器引擎骨架可用，孤儿 filter 清理已验证 |
| 过滤层选择依据 | 见 [architecture.md](../architecture.md) 第 3 节 |
| 主动断连的边界 | IPv4 用 `SetTcpEntry`，IPv6 本阶段不做即时断连 |
| 平台相关实现 | 一律经 `IFilterEngine` 与 `IKiller`，不得在核心层直接调用系统接口 |
| WFP 层与条件 GUID | **工具链不自带，需自行维护常量表**，见第 2.1 节 |
| 过滤器枚举的约束 | 枚举模板必须同时给出 provider 与层，见第 2.1 节 |

### 2.1 实施 S1.7 时挖出来的两个平台事实

这两条都由实测确认，不是推断。它们直接决定 S2 的代码怎么写，因此提前记在这里。

**其一：MinGW 的 WFP 头没有层与条件字段的 GUID。**

`fwpmu.h` 只 `DEFINE_GUID` 了三个 provider 常量，`FWPM_LAYER_*` 与 `FWPM_CONDITION_*`
**一个都没有**；`libfwpuclnt.a` 也只导出函数，不导出这些 GUID 符号。
所以「不需要 Windows SDK」这个结论仍然成立，但代价是：**层与条件的 GUID 必须自己维护一张常量表**，
取值来自微软文档（属于 WFP 的 ABI，不会变）。

维护这张表有两条纪律：

1. **取值必须逐个校验**，不能照记忆写。校验办法是现成的：
   `FwpmLayerEnum0` 会列出系统里全部层的 `layerKey`，拿候选值去比对即可。
   `tmp/probe_wfp_layers.cpp` 就是干这个的。已确认可用的四个值是
   `ALE_AUTH_CONNECT_V4`、`ALE_AUTH_CONNECT_V6`、`ALE_AUTH_RECV_ACCEPT_V4`、`ALE_AUTH_RECV_ACCEPT_V6`。
   S2.6 要用到的入站 UDP 数据报层与 S2.7 的传输层兜底，各自在第一次用到时按同样办法确认。
2. **层名不能用来认层**。系统里层的 `displayData.name` 是资源串或短分类名
   （实测取到的是 `ALE`、`IP`、`RPC` 这类，甚至空串），靠名字区分不出是哪一层。

写错层 GUID 的后果很隐蔽：过滤器会落在别的层上，表现成「规则明明下发了却不起作用」，
而且不会报错。所以校验这一步不能省。

**其二：枚举过滤器必须同时给出 provider 与层。**

`FwpmFilterCreateEnumHandle0` 的模板里，`layerKey` 留空会被 WFP 以
`FWP_E_LAYER_NOT_FOUND`（`0x80320004`）拒绝；只给 `providerKey` 不够。

因此「找出本工具的全部过滤器」只能是：先 `FwpmLayerEnum0` 拿全部层，
再逐层用本工具的 `providerKey` 过滤一遍。

**不要改成写死一份层清单**：清单必须随功能扩展而增补，只要有一次忘了补，
那一层上的旧过滤器就永远清理不掉，而且清理还会报告成功 —— 是静默失败。
逐层问一遍的代价是启动时多几十毫秒，换的是清理的完整性不依赖人记得改。

另外一个结构细节：`FWPM_FILTER_ENUM_TEMPLATE0::providerKey` 与 `FWPM_FILTER0::providerKey`
是 `GUID*` 而不是值，要传地址；常量是 `const`，得先拷一份可写的出来。

## 3 步骤清单

| 步骤 | 内容 | 产出物 | 验收 | 依赖 | 并行 |
| --- | --- | --- | --- | --- | --- |
| S2.1 | 匹配条件式的规则模型与校验：域与匹配方式的定义、各域值解析、任意条件取反、非法输入拒绝 | 规则模型模块 | 非法输入被拒绝且错误信息明确；空条件规则无法保存 | S1.2 | 否，是 S2.2 前提 |
| S2.2 | 规则到过滤器的展开器：对各条件求值后展开为过滤器并构造过滤条件，含求值器注册表 | 条件构造模块 | 给定规则能输出确定的过滤器清单，含数量与条件 | S2.1 | 否 |
| S2.3 | 目标进程解析：可执行文件路径到设备路径的转换、进程存活跟随、短路径与大小写处理 | 目标解析模块 | 系统盘、非系统盘、应用商店目录三类路径均正确 | S1.2 | 是，与 S2.1 并行 |
| S2.4 | 下发与撤销：事务批量提交、过滤器与规则的双向映射、重复下发幂等、失败回滚 | 下发模块 | 重复下发不产生重复过滤器，撤销后无残留 | S2.2 | 否 |
| S2.5 | 出站封禁：在连接授权层生效，覆盖 IPv4 与 IPv6、TCP 与 UDP | 出站策略 | 目标进程的新连接建不起来 | S2.4 | 是 |
| S2.6 | 入站封禁：在接收授权层生效，并为入站 UDP 追加数据报层 | 入站策略 | 外部无法连入目标端口 | S2.4 | 是 |
| S2.7 | 端口与网段细分：远端端口、本地端口、多网段与 IPv6 段 | 条件扩展 | 按端口与网段封禁精确命中，且不误伤同进程其他连接 | S2.4 | 是 |
| S2.8 | IPv4 即时断连：删除传输控制块，并严格保证先封后断的时序 | 断连模块 | 已建立的 IPv4 连接被立即中断且不再重连 | S2.5 | 是 |
| S2.9 | 规则持久化与恢复：启动时按数据库重建过滤器，维护启用状态与过期时间 | 规则仓储 | 重启后规则自动生效，过期规则自动失效 | S2.4 | 是 |
| S2.10 | 一次性验证脚本：在 `tmp/` 下核对过滤器数量与生效情况 | 验证脚本 | 脚本可独立复现封禁与撤销，不依赖界面 | S2.5 | 是 |
| S2.11 | 程序域通配匹配：`wildcard` 模式的解析与命中判定，含宽泛通配的识别 | 通配求值器 | 通配规则能正确命中，且宽泛写法被标记为高风险 | S2.2 | 是 |
| S2.12 | 程序域目录匹配：`dir` 模式的解析与命中判定，含目录内容变化后的行为 | 目录求值器 | 目录内新增可执行文件按预期生效 | S2.2 | 是 |

## 4 并行编组

| 编组 | 步骤 | 并行安全性依据 |
| --- | --- | --- |
| A 主线 | S2.1 到 S2.2 到 S2.4 | 规则表达必须先于条件构造，条件构造必须先于下发 |
| B 独立支线 | S2.3 | 只依赖冻结接口，与主线无文件重叠 |
| C 能力面 | S2.5、S2.6、S2.7、S2.8 | 都在 S2.4 之后，分别操作不同过滤层与不同代码路径 |
| D 收尾 | S2.9、S2.10 | 依赖主干能力已成，两者彼此独立 |
| E 匹配扩展 | S2.11、S2.12 | 只依赖条件构造，主干完成后可与其他轨道并行 |

## 5 阶段验收

1. 可对指定进程封 IPv4 与 IPv6 的出站与入站，TCP 与 UDP 均有效
2. 可按远端端口、本地端口与网段精确限定范围
3. 匹配条件支持任意条件取反，且取反后的实际语义与「人话翻译」一致
4. 空条件规则被拒绝保存，要表达「全部」必须显式写出全匹配条件
5. IPv4 已建立连接能被立即中断，且不会立刻重连成功
6. 规则撤销后立即恢复，且无残留过滤器
7. 重启后规则自动重建，过期规则不再生效
8. 连续增删同一规则一百次后，过滤器总数回到初始值
9. 未知的匹配域或匹配方式会让该规则被拒绝加载并报错，而不是被宽松解释

## 6 风险与对策

| 风险 | 对策 |
| --- | --- |
| 条件构造错误导致封禁范围过大 | 展开器必须可确定性输出，用 S2.10 的脚本核对数量与条件，不靠目测 |
| 取反条件让规则变宽而用户没察觉 | 规则必须渲染成一句中文，宽条件要被显式标记 |
| 通配写宽了命中意料之外的程序 | 通配规则默认带风险标记，可配合演练模式先行验证 |
| 断连后对端立刻重连 | 把「先封后断」写成不变量并纳入验收，而不是留在注释里 |
| 地址族或过滤层选错 | 每个能力点都要求 IPv4 与 IPv6 各测一次，不合并验收 |
| 路径转换在特殊卷上失败 | 强制覆盖三类路径，见 S2.3 的验收标准 |

## 7 交付物清单

- 匹配条件式的规则模型与校验器，含任意条件取反
- 规则到过滤器的展开器
- 条件求值器注册表，支持后续扩展新域
- 完整的过滤器下发与撤销能力
- 出站、入站、端口与网段、IPv4 断连四类能力
- 程序域的通配与目录匹配
- 规则仓储与启动恢复
- `tmp/` 下的一次性验证脚本
