# 阶段一 工程骨架与运行基座

## 1 目标与范围

产出一个「什么都能装、什么都还没做」的程序：能提权启动、单实例、能读写配置与数据库、
能记录日志、能干净退出，并且 WFP 引擎可以安全地打开与关闭。

本阶段**不实现任何封禁逻辑**，只把地基与接口定死。

## 2 前置条件

| 项 | 状态 |
| --- | --- |
| Qt 套件 | 已就绪，含 Qt Quick、Quick Controls、Svg、Charts |
| 编译器与构建 | Qt 套件自带的 MinGW 与 CMake、Ninja 均可用 |
| 绝对路径 | 见环境文档（`tmp/` 下，不入库） |
| WFP 头文件与导入库 | 已核实齐全，无需 Windows SDK |
| 管理员权限 | 需在本阶段实际验证提权清单确实生效 |

## 3 步骤清单

| 步骤 | 内容 | 产出物 | 验收 | 依赖 | 并行 |
| --- | --- | --- | --- | --- | --- |
| S1.1 | 建 CMake 工程骨架与目录结构，锁定工具链与编译选项，跑通空窗口 | CMakeLists、CMakePresets、目录骨架 | 本机一条命令构建通过并弹出空窗口 | 无 | 否，最先 |
| S1.2 | 冻结内部接口，只写头文件与语义注释 | 核心与平台两层接口头文件，清单见 [README 第 3.2 节](../README.md) | 头文件可编译、语义无歧义，且头文件数量与冻结清单一致 | S1.1 | 否，是并行前提 |
| S1.3 | 提权与单实例：清单声明 requireAdministrator、命名 Mutex、二次启动唤起既有实例 | 清单文件、单实例模块 | 双击可提权启动，二次启动不产生第二个实例 | S1.2 | 否 |
| S1.4 | 日志子系统：级别、文件输出、单文件滚动与总量上限 | 日志模块 | 日志可滚动且不会无限增长 | S1.2 | 是 |
| S1.5 | 配置子系统：JSON 落盘于 `%APPDATA%\BanIPHelper`，含默认值、类型校验、变更通知 | 配置模块与默认配置 | 删掉配置文件可自动重建默认值 | S1.2 | 是 |
| S1.6 | 数据库子系统：建表、迁移版本号、WAL 模式、批量写入接口 | 数据模块与建表语句 | 首次启动自动建库，二次启动不重复建表 | S1.2 | 是 |
| S1.7 | WFP 引擎最小封装：打开引擎、注册自有 provider 与 sublayer、枚举并清理孤儿 filter、安全关闭 | 过滤器引擎骨架 | 启动时能列出并清理上次残留的自家 filter | S1.2 | 否，与 S1.3 串联 |
| S1.8 | 托盘外壳：图标、菜单、退出即撤销开关、退出路径 | 托盘模块 | 托盘菜单可退出且进程干净结束 | S1.3 | 否 |
| S1.9 | 异常路径演练：正常退出、强杀进程、模拟崩溃后重启 | 演练记录 | 三种情况重启后均无残留 filter | S1.7 | 否 |
| S1.10 | 平台抽象层骨架与隔离约束：建立 `platform/api` 纯接口目录与 `platform/win` 实现目录，加入「核心层不得引用平台专有头」的构建期检查 | 目录骨架与检查脚本 | 故意在核心层引入平台头会构建失败 | S1.2 | 是 |
| S1.11 | 能力协商机制：能力位集合的定义、声明与查询，以及不可用能力的禁用与错误码约定 | 能力协商模块 | 查询任一能力可得到明确的是否可用，且无静默失败路径 | S1.2 | 是 |
| S1.12 | 最小后端装配点：平台无关的装配入口与编译期后端选择，使上层不必引用平台专有头就能拿到平台实现 | 装配入口与后端目标 | 换平台只改构建配置，核心层与界面层零改动 | S1.2 | 是 |
| S1.13 | 契约测试套件与内存后端的最小版本：先覆盖提权与单实例两个接口，真实后端与内存后端跑同一套断言 | 契约测试套件、内存后端 | 两个后端在同一套断言下全部通过，且不存在被静默跳过的分支 | S1.2 | 是 |
| S1.14 | Qt 运行库随构建部署：把依赖放到可执行文件旁边 | 构建步骤 | 不设置任何环境变量时，双击即可启动 | S1.3 | 否 |
| S1.15 | WFP 常量 GUID 表：层与条件的取值、出处标注、防抄错的编译期断言，以及一份把表拿到本机运行时逐条核对的工具 | 常量表头文件与核对工具 | 表里每个层常量都能在本机 `FwpmLayerEnum0` 的结果里找到 | S1.7 | 是 |

### 3.1 追加步骤的原因

S1.12 到 S1.15 是实施过程中按[变更控制](../README.md#6-变更控制)追加的步骤，
不是原计划的一部分。它们都不是随手加的功能，而是前一步逼出来的前提：

- **S1.12 与 S1.13** 来自项目自己定的硬顺序（[README.md](../README.md) 第 8.1 节）：
  只要开始写第一个平台实现，就必须同时补内存实现与契约测试。而 S1.3 的验收里
  「二次启动不产生第二个实例」要求界面层拿得到平台实现，于是装配点也不得不先有。
  两者分别是阶段八 S8.1 与 S8.2 的最小版本，完整形态仍留在阶段八。
- **S1.14** 是实施提权清单时暴露的：程序一旦带 `requireAdministrator`，
  经 UAC 提权启动时 Windows 使用的是用户注册表里的环境，
  **不继承开发终端临时加进 PATH 的 Qt 目录**，加载器找不到 `Qt6Gui.dll`，
  程序在 `main` 之前就失败，现象与代码错误完全不像。因此依赖必须放在可执行文件旁边。
- **S1.15** 是实施 S1.7 之后补的。S1.7 靠「运行时枚举全部层」绕开了
  「MinGW 的 `fwpmu.h` 不含层 GUID 常量」这个缺口，但那条路只解决**清理**，
  解决不了**下发** —— 建过滤器必须给出具体的层 GUID。
  与其等到 S2.4 边写边猜，不如先把取值、出处、校验手段和防抄错的编译期断言一次立好。
  代价很小（一张常量表加一个核对工具），买到的是
  「S2 的规则不会因为一个抄错的 GUID 而静默失效」。

## 4 并行编组

| 编组 | 步骤 | 并行安全性依据 |
| --- | --- | --- |
| A | S1.4、S1.5、S1.6 | 三者只依赖冻结接口，模块与文件互不重叠 |
| B | S1.3 到 S1.9 的串联链 | 依次触及权限、过滤器引擎、退出路径，必须串行 |
| C | S1.10、S1.11 | 只涉及目录约束与能力声明，与编组 A、B 均无文件重叠 |
| D | 编组 A、B、C 三者 | 可同时进行，唯一接触点是接口定义，已在 S1.2 冻结 |

## 5 阶段验收

1. 双击启动会请求提权，且启动后确实具备管理员权限
2. 二次启动不产生第二个实例，而是唤起既有托盘
3. 配置文件与数据库可自动创建、可读写、可重建
4. 启动时能清理上一次残留的自家 filter，且不会误删其他工具的 filter
5. 正常退出、强杀、崩溃重启三种情况均无残留 filter
6. 全程日志可查，且日志文件不会无限增长
7. 核心层与界面层均不引用任何平台专有头，违反时构建直接失败
8. 能力协商可查询，且不存在静默失败的能力路径

## 6 风险与对策

| 风险 | 对策 |
| --- | --- |
| 提权进程难以被调试器附加 | 日志成为主要排查手段，因此 S1.4 的优先级要提前 |
| 清理孤儿 filter 时误删他方 filter | 只以自有的 provider 与 sublayer 作为归属判据，绝不全量清理 |
| 接口冻结不彻底，后续被迫改动 | S1.2 的验收标准是「实现可以没有，签名与语义必须无歧义」，必要时先写用例再定签名 |
| 平台抽象层被绕过，导致后期无法移植 | S1.10 的构建期检查是硬约束，代码评审时对核心层出现平台概念一律打回 |

## 7 交付物清单

- 可构建的 CMake 工程与目录骨架
- 两层接口头文件：核心接口与平台接口
- 平台抽象层目录骨架与「禁止引用平台头」的构建期检查
- 能力协商模块
- 平台无关的后端装配入口与编译期后端选择
- 提权与单实例两个接口的契约测试套件与内存后端实现
- Qt 运行库的构建期部署
- 日志、配置、数据库三个子系统
- 托盘外壳与单实例机制
- WFP 引擎骨架与孤儿 filter 清理
- 异常路径演练记录
